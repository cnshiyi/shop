# Shop 自动优化最新状态

本文件由自动化每轮覆盖更新，供快速复查当前状态。详细历史记录继续追加到 `docs/refactor-version-record.md`。

## 最近一轮

- 时间：2026-06-08 22:18 CST
- 状态：完成生命周期闭环开关矩阵和任务队列只读巡检；本轮未发现需要修代码的问题。
- 后端提交：本轮准备提交巡检记录。
- 前端提交：本轮无前端代码变更。

## 本轮覆盖范围

- 后端仓库：`/Users/a399/Desktop/data/shop`
- 重点链路：
  - 关机总开关
  - 服务器删除总开关
  - IP 删除总开关
  - 资产单项关机开关
  - 资产单项服务器删除开关
  - 资产单项 IP 删除开关
  - 生命周期任务表 `CloudLifecycleTask`
  - 计划页首屏 `queue_status`

## 全局开关

当前真实库配置：

- `cloud_server_shutdown_enabled=True`
- `cloud_server_delete_enabled=True`
- `cloud_ip_delete_enabled=True`

当前时间窗口：

- 当前本地时间：`2026-06-08 22:12 CST`
- 关机执行窗口：`False`
- 服务器删除执行窗口：`False`
- IP 删除执行窗口：`False`

结论：

- 三个生命周期总开关当前都开启。
- 但当前不在任何执行窗口内，执行器即使被触发也应被时间窗口拦截。

## 资产单项开关分布

真实库统计：

- 服务器资产总数：`2500003`
- 关机单项开关关闭：`104612`
- 服务器删除单项开关关闭：`4612`
- 服务器资产上的 IP 删除单项开关关闭：`4612`
- 未附加 IP 识别总数：`600001`
- 未附加 IP 的 IP 删除单项开关关闭：`0`
- 未附加 IP 条件命中但仍有 `instance_id` 的资产：`100000`

说明：

- 执行器的未附加 IP 释放入口仍会检查 `asset.instance_id`，有关联实例时不会按未附加 IP 删除。
- 本轮只统计，不修改任何资产开关。

## 生命周期任务队列

`CloudLifecycleTask` 当前状态：

- `suspend/done=5`
- `delete/done=6`
- `recycle/done=5`
- 没有 `pending` / `claimed` / `failed` 的到期开放任务。

结论：

- 当前没有正在等待执行、已认领或失败重试中的到期生命周期任务。
- 历史任务均为完成状态。

## 计划页状态抽样

计划页首屏 `queue_status` 分布：

- 关机计划首屏：
  - `due_now=45`
  - `shutdown_disabled=5`
- 服务器删除计划首屏：
  - `due_now=2`
  - `within_window=44`
  - `server_delete_disabled=4`
- IP 删除计划首屏：
  - 轻量字段请求未返回 `queue_status`，本轮未据此判断 IP 状态。

服务器删除计划阶段复核：

- 删除计划查询层第 1 页资产均有关联订单。
- 抽样分布：
  - `asset=running / order=deleting`：`2`
  - `asset=running / order=suspended`：`18`
- 这表示删除计划是按订单阶段进入删除队列；资产云端状态仍需执行器真实删机后同步收敛。

## 执行器代码复核

本轮只读复核确认：

- 关机执行入口检查关机总开关、资产关机开关、计划时间和关机执行窗口。
- 服务器删除执行入口检查删除总开关、资产服务器删除开关、计划时间和删除执行窗口。
- 订单固定 IP 回收入口检查 IP 删除总开关、资产 IP 删除开关、计划时间和 IP 删除执行窗口。
- 未附加 IP 释放入口检查 IP 删除总开关、资产 IP 删除开关、是否仍有关联实例、计划时间和 IP 删除执行窗口。

## 本轮验证

通过：

```bash
UV_CACHE_DIR=/private/tmp/uv-cache-shop uv run python manage.py check
git diff --check
```

说明：

- 本轮没有修改运行代码，因此未新增聚焦测试。
- 本轮没有执行真实关机、删机、IP 释放、支付、链上广播、生产发布或删除业务数据。
- `docs/real-machine-test-report.md` 当前仍存在既有未提交真实机器测试记录，本轮不覆盖、不提交。

## 结论

- 生命周期开关矩阵读侧正常：三个总开关已分离，资产单项开关已分离。
- 当前不在执行窗口，开放到期任务队列为空。
- 删除计划按订单阶段进入队列，实际删除仍受总开关、单项开关和执行窗口保护。

## 剩余风险

- 真实云资源创建后的完整关机、删机、IP 释放闭环仍需继续在授权范围内逐项验证。
- 机器人多任务高并发真机点击压测仍未完成；需要 Telegram Bot API 和 MTProto 恢复可连后继续。
- 当前仍有既有真机报告脏文件 `docs/real-machine-test-report.md`，需要单独处理，不应混入本轮巡检记录提交。
