# Overnight Task List - 2026-04-15

## User Requirements

1. Telegram 多用户名/用户信息同步
   - 每次用户使用机器人都同步用户信息。
   - 用户可能改名字、改用户名，但 Telegram ID 不变。
   - 需要保存历史用户名/多用户名，并更新当前昵称/用户名。

2. 云资产列表改造
   - 移除列表字段：类型、来源、云厂商、实例 ID、代理端口。
   - 增加代理链接字段。
   - 代理链接写入完成安装 MTProxy 后的实际链接。
   - 修复阿里云到期时间获取。
   - 增加到期时间编辑。
   - 添加价格展示，价格可后台修改。
   - 后续要支持续费开发。
   - 增加用户关联，可根据用户名或用户 ID 关联用户。
   - 增加用户 ID 写入口，后台根据用户 ID 自动查找。
   - 按用户 ID 分组展示，分组可折叠，默认展开。
   - 分组显示用户姓名。
   - 添加排序方式，快到期用户优先展示。

3. 服务器表
   - 新增服务器表。
   - 后期可能增加多用户、多账户，需要区分账户/来源。
   - 记录服务器各种信息。
   - 服务器到期时间要与云资产列表同步。
   - 支持调取 API 同步所有服务器。

4. 权限修复
   - 修复编辑余额权限不足。

5. 前后端
   - 前后端同步改造到可用状态。
   - 尽量验证。

## Execution Priorities

1. 后端模型/API/同步逻辑。
2. 机器人用户资料同步。
3. Admin 和 dashboard API。
4. Vben 前端云资产列表分组/编辑。
5. 验证与风险清单。

## Watchdog

- Cron job: `shop-overnight-watchdog`
- Frequency: every 30 minutes
- Purpose: check and resume if stuck.
